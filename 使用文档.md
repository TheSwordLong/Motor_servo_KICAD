# GD32F303VET6 四电机接口驱动控制板工程使用文档

## 1. 工程简介

本工程为基于 GD32F303VET6 的四层四电机接口驱动控制板，包含 MCU 核心、电源、USB 转串口、CAN、RS485、RS232、电机通信、VBAT 后备电池以及 GND/Earth 隔离与泄放网络。

主工程文件：

- `GD32F303VET6_4Motor_Control.kicad_pro`：KiCad 工程入口。
- `GD32F303VET6_4Motor_Control.kicad_sch`：顶层原理图。
- `GD32F303VET6_4Motor_Control.kicad_pcb`：PCB 设计文件。
- `GD32F303VET6.kicad_sch`：MCU 核心子原理图。
- `POWER.kicad_sch`、`CAN.kicad_sch`、`RS485.kicad_sch` 等：各功能子模块原理图。

建议使用 KiCad 10.0 或兼容版本打开工程。

## 2. 打开与保存工程

1. 双击 `GD32F303VET6_4Motor_Control.kicad_pro` 打开工程管理器。
2. 从工程管理器进入原理图编辑器或 PCB 编辑器。
3. 如果 PCB 文件曾被外部脚本或版本控制更新，应先关闭旧的 PCB 编辑器窗口，再重新打开，避免旧窗口覆盖磁盘上的新版文件。
4. 保存前先运行 DRC，并确认没有新出现的短路、间距或未连接错误。

## 3. PCB 基本参数

- 板框尺寸：约 100 mm × 60 mm，圆角外形。
- 铜层数量：4 层。
- 器件数量：约 127 个封装。
- 焊盘数量：约 510 个。
- 网络数量：约 198 个。

推荐层叠用途：

| 层 | 用途 |
| --- | --- |
| F.Cu | 主要器件面、短距离信号、电源颈缩、局部 GND/电源铜皮 |
| In1.Cu | 连续 GND 参考平面，禁止普通信号穿行 |
| In2.Cu | 信号与电源主干走线 |
| B.Cu | 辅助信号走线、跨区连接及底层 GND 铜皮 |

## 4. 线宽、间距与过孔规则

当前设计采用以下基础规则：

| 网络类型 | 推荐线宽 | 推荐间距 | 过孔直径/孔径 |
| --- | ---: | ---: | ---: |
| 普通信号 | 0.20 mm | 0.20 mm | 0.60/0.30 mm |
| 密集引脚短颈 | 0.15 mm | 0.20 mm | 按实际空间选择 |
| +3.3V、+5V、VBAT 等电源 | 0.50 mm 主干 | 0.20 mm | 0.80/0.40 mm |
| CAN、RS485 | 0.25 mm | 0.20 mm | 0.60/0.30 mm |
| USB 差分线 | 0.20 mm | 按实际板厂叠层计算 | 0.60/0.30 mm |

注意：USB 差分阻抗必须结合最终板厚、铜厚、介质厚度和介电常数重新计算，不能只依赖固定线宽。

## 5. GND、GNDS 与 Earth 处理

工程中存在三个不同用途的地网络：

- `GND`：主控制和数字系统地。
- `GNDS`：隔离侧地。
- `Earth`：机壳地、屏蔽地或保护地。

三者不得通过铺铜直接短接。

`GND` 与 `Earth` 之间通过磁珠、电容和两个 1 MΩ 电阻组成可控的高频泄放与静电释放通路。相关器件包括 FB2、C51、C11 和 R6。修改 PCB 时应保留网络名称和连接关系，不要用铜皮跨接两侧。

推荐铺铜方式：

1. In1.Cu 使用整面 `GND` 铜区，距板边至少 0.50 mm。
2. F.Cu、B.Cu 可铺 `GND` 铜皮，但要删除悬空孤岛。
3. `GNDS` 只在隔离侧局部铺铜，不跨越隔离器件的安全间距。
4. `Earth` 仅在连接器屏蔽、安装孔及泄放网络附近使用，不扩展到 MCU 区域。
5. 每次移动器件或走线后，在 PCB 编辑器中按 `B` 重新填充全部铜区。

## 6. VBAT 电池座

VBAT 电池座 B01 已放置在 PCB 背面，中心位置约为 `(162.3065 mm, 98.9350 mm)`。

检查要求：

- 确认电池座本体与正面器件不存在机械干涉。
- 检查 B.Silkscreen 与相邻器件丝印是否重叠。
- 电池座周围不要布置容易被电池外壳压到的高器件。
- 更换封装或电池型号后，应重新检查 3D 外形和装配高度。

## 7. DRC 检查流程

1. 打开 PCB 编辑器。
2. 按 `B` 重新填充铜区。
3. 选择“检查 → 设计规则检查器”。
4. 先处理以下错误：
   - 不同网络短路。
   - 走线、过孔或铜区间距不足。
   - 有效信号或电源网络未连接。
   - 铜到板边间距不足。
   - 走线宽度小于制造能力。
5. 再处理丝印压焊盘、丝印越板边、庭院重叠和文字尺寸等装配问题。

部分 USB-C 连接器封装内部的机械孔、无网络铜图形和焊盘间距可能产生封装级 DRC 报告。此类问题不能直接忽略，应与连接器数据手册和板厂能力核对后再决定修改封装或建立局部规则。

## 8. 生产文件导出

提交生产前至少完成以下检查：

1. 确认板框仅存在于 `Edge.Cuts`，且为闭合轮廓。
2. 确认 `Margin` 或 `User.Drawings` 中的辅助矩形不会被当作板框。
3. 运行完整 DRC，并保存检查结果。
4. 使用 Gerber 导出以下层：
   - F.Cu、In1.Cu、In2.Cu、B.Cu
   - F.Mask、B.Mask
   - F.Silkscreen、B.Silkscreen
   - Edge.Cuts
5. 导出 Excellon 钻孔文件，并区分 PTH/NPTH（如果板厂要求）。
6. 使用 Gerber 查看器复查所有铜层、阻焊、丝印、板框和钻孔。
7. 生成 BOM 和坐标文件前，确认器件面的定义以及 B01 等底层器件的旋转方向。

## 9. Git 提交建议

提交前执行：

```text
git status
git diff --stat
```

建议将原理图修改、布局修改、布线铺铜和生产文件分别提交，便于后续定位问题。布线与铺铜版本可使用类似提交消息：

```text
完成四层板布线并添加 GND 与局部电源铜区
```

不要提交临时 DSN、SES、DRC JSON、路由器日志或测试渲染图片。

## 10. 当前注意事项

- PCB 布线与铺铜完成后仍需以最终 KiCad DRC 结果为准。
- 自动布线结果必须人工复核晶振、USB 差分线、电源回路、隔离区和连接器附近走线。
- 晶振走线应尽量短、同层、少过孔，并远离高速或大电流开关节点。
- DC/DC 的输入电容、开关节点、续流器件和电感回路应优先人工优化。
- CAN、RS485 的终端电阻和保护器件应靠近接口放置。
- 在生成 Gerber 前，应再次检查 VBAT 电池座的背面装配空间和丝印。
